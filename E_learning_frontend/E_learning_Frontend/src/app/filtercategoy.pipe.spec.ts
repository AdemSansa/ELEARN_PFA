import { FiltercategoyPipe } from './filtercategoy.pipe';

describe('FiltercategoyPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercategoyPipe();
    expect(pipe).toBeTruthy();
  });
});
