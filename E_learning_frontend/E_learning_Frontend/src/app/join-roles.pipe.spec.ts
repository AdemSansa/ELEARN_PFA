import { JoinRolesPipe } from './join-roles.pipe';

describe('JoinRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
